# ========================================
# VARIÁVEIS DE AMBIENTE - RENDER DEPLOY
# ========================================

# Ambiente
NODE_ENV=production

# Porta do Servidor (Render define automaticamente)
PORT=5000

# Database (PostgreSQL)
# No Render: use o Internal Database URL fornecido pelo PostgreSQL
DATABASE_URL=postgresql://user:password@host:5432/database

# Segurança
# IMPORTANTE: Gere um secret forte para produção
# Use: openssl rand -base64 32
JWT_SECRET=your_super_secret_jwt_key_change_this_in_production

# CORS - Domínios permitidos (separados por vírgula)
# Adicione seu domínio do Render aqui
ALLOWED_ORIGINS=https://seu-app.onrender.com,https://www.seudominio.com

# WebSocket (Opcional - apenas se usar SSL customizado)
# Para Render, deixe em branco - não é necessário
WS_PORT=5001
SSL_CERT_PATH=
SSL_KEY_PATH=

# ========================================
# INSTRUÇÕES PARA CONFIGURAR NO RENDER
# ========================================
# 
# 1. Crie um PostgreSQL Database no Render
# 2. Copie o "Internal Database URL" 
# 3. Cole como valor de DATABASE_URL
# 4. Gere um JWT_SECRET forte usando:
#    openssl rand -base64 32
# 5. Configure ALLOWED_ORIGINS com seu domínio do Render
# 6. NODE_ENV já virá como production automaticamente
# 7. PORT será definido automaticamente pelo Render
#
# ========================================
